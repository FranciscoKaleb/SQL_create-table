CREATE DATABASE inventory_system;

USE inventory_system;

CREATE TABLE users(
	id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) UNIQUE,
    password_ VARCHAR(50)
);

CREATE TABLE products(
	id INT AUTO_INCREMENT PRIMARY KEY,
    product_name VARCHAR(50),
    category VARCHAR(50),
    price DECIMAL(10, 2),
    stock INT, 
    added_by INT,
    FOREIGN KEY (added_by) REFERENCES users(id)
);


SELECT * FROM products;
SELECT * FROM users;

INSERT INTO products(product_name, category, price, stock) VALUES('fita', 'bread', 10, 100);
INSERT INTO products(product_name, category, price, stock) VALUES('Milo', 'powdered drink', 12, 200);

UPDATE products SET product_name = 'MILO' WHERE id = 7;

DELETE FROM products WHERE id = 1;






-- simple login query
SELECT * FROM users WHERE username = 'kaleb' AND password_ = '1234';

-- select only products added by a specific user
SELECT * FROM products WHERE added_by = 1;
